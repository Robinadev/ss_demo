export enum CaseStatus {
  PENDING = 'pending',
  ASSIGNED = 'assigned',
  RESOLVED = 'resolved',
  CLOSED = 'closed',
}

export enum UserRole {
  ADMIN = 'admin',
  INSPECTOR = 'inspector',
  USER = 'user',
}

export enum Priority {
  LOW = 'low',
  MEDIUM = 'medium',
  HIGH = 'high',
  CRITICAL = 'critical',
}
